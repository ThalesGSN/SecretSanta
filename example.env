# This is an example environment file.
# Copy this to a new file named .env and fill in your actual credentials.
# Do NOT commit the .env file to version control.

# --- random.org API Key ---
# Get your key from https://api.random.org/api-keys
RANDOM_ORG_API_KEY="your-random-org-api-key"

# --- Event Details for the Email Template ---
EVENT_DATE="December 25, 2024"
EXPECTED_VALUE="$25"
PLACE="The North Pole"
ORGANIZER_EMAIL="organizer@example.com"

# --- SMTP Server Details for Sending Email ---
# Example for Gmail. Use your own email provider's settings.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
# The script will securely prompt for the SMTP password, so it is not stored here.
